document.addEventListener('DOMContentLoaded', () => {
    // Small Claims Court specific JavaScript

    // Currently, there are no specific interactive elements
    // defined in the small-claims-court.php content that require JS.
    // This file is included for consistency and future enhancements.

    // Example: You could add smooth scrolling to section IDs if needed
    // setupSmoothScrolling();

    console.log("Small Claims Court JS loaded.");
});

// Example function for smooth scrolling (if needed later)
// function setupSmoothScrolling() {
//     document.querySelectorAll('a[href^="#"]').forEach(anchor => {
//         anchor.addEventListener('click', function (e) {
//             e.preventDefault();
//             const targetId = this.getAttribute('href');
//             const targetElement = document.querySelector(targetId);
//             if(targetElement) {
//                 targetElement.scrollIntoView({
//                     behavior: 'smooth'
//                 });
//             }
//         });
//     });
// }